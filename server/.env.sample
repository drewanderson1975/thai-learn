# Google Cloud Text-to-Speech server configuration
# Copy this file to .env and adjust values as needed

# Port the Express TTS server will listen on
PORT=8787

# Path to your Google Cloud service account key JSON file (outside version control)
GOOGLE_APPLICATION_CREDENTIALS=../secrets/google-tts-service-account.json

# (Optional) Explicit Google Cloud Project ID (SDK may infer automatically)
GOOGLE_PROJECT_ID=your-gcp-project-id

# (Optional) Cache TTL (ms) for voices list; default falls back to in-memory lifetime
# VOICES_CACHE_TTL=3600000

# (Future) Basic auth token or API key placeholder (do NOT use in production as-is)
# API_KEY=change-me
